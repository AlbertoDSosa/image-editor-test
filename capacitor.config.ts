import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.beatandcode.imagedit',
  appName: 'image-edit',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
